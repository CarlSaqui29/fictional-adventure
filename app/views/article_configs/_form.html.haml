= form_for @article_config do |f|
  - if @article_config.errors.any?
    #error_explanation
      %h2= "#{pluralize(@article_config.errors.count, "error")} prohibited this article_config from being saved:"
      %ul
        - @article_config.errors.full_messages.each do |message|
          %li= message

  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.label :description
    = f.text_area :description
  .actions
    = f.submit 'Save'
